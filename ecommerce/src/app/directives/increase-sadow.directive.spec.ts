import { IncreaseSadowDirective } from './increase-sadow.directive';

describe('IncreaseSadowDirective', () => {
  it('should create an instance', () => {
    const directive = new IncreaseSadowDirective();
    expect(directive).toBeTruthy();
  });
});
